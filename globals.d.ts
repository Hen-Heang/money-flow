// Ambient declarations for side-effect style imports (e.g. `import './globals.css'`).
// Next.js provides these via its own types, but declaring them explicitly keeps the
// editor's TS server from flagging TS2882 under `moduleResolution: "bundler"`.
declare module '*.css'
declare module '*.scss'
declare module '*.sass'
